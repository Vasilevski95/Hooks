// import React, { useEffect, useState } from "react";

// const useFetch = (url) => {
//   const [responses, setResponses] = useState([]);

//   useEffect(() => {
//     fetch(url)
//       .then((res) => res.json())
//       .then((data) => setResponses(data));

//     //Here i fetch data from this API, and convert it to JSON format
//     //And then set that data inside responses (setResponses)
//   }, []);

//   return responses;
// };

// export default useFetch;
